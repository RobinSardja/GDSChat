name: gdsnapchat
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  camera: ^0.10.5+5
  flutter:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1
  path: ^1.8.3
  path_provider: ^2.1.1
  share_plus: ^7.2.1
  url_launcher: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

flutter_launcher_icons:
  image_path: 'assets/GDSnapChat icon.png'
  android: true
  ios: true